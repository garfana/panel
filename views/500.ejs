<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Internal Server Error</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Tailwind -->
  <link rel="stylesheet" href="./assets/tailwind.css">
</head>

<body class="min-h-screen bg-[#0f1320] flex items-center justify-center text-white">

  <div class="max-w-xl w-full text-center px-6">
    <h1 class="text-7xl font-bold text-orange-400 mb-4">500</h1>

    <h2 class="text-2xl font-semibold mb-3">
      Internal Server Error
    </h2>

    <p class="text-gray-400 mb-8">
      Something went wrong on our side. Please try again later.
    </p>

    <% if (typeof err !== "undefined") { %>
      <div class="bg-black/40 border border-white/10 rounded-lg p-4 text-left text-sm text-gray-300 mb-6 overflow-auto">
        <pre><%= err.stack || err.toString() %></pre>
      </div>
    <% } %>

    <a
      href="/"
      class="inline-block px-6 py-2.5 rounded-xl bg-orange-500 hover:bg-orange-600 transition font-medium text-white"
    >
      Go Home
    </a>
  </div>

</body>
</html>
